<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quintaslopeznereaproyectopmdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuintasLopezNereaProyectoPMDM">
        <activity
            android:name=".modelo.dao.PeliculasDaoMockImpl"
            android:exported="true" />
        <activity
            android:name=".adapters.ListaPeliculasAdapter"
            android:exported="true" />
        <activity
            android:name=".modelo.entidades.Pelicula"
            android:exported="true" />
        <activity
            android:name=".activities.ListadoActivity"
            android:exported="true" />
        <activity
            android:name=".activities.RegistroActivity"
            android:exported="true">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.LoginActivity" />
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>